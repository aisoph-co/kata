[project]
name = "hermes-kata"
version = "0.1.0"
description = "Hermes plugin: the entire integration surface between Hermes and the Kata learning service."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
