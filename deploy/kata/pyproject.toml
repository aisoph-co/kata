[project]
name = "kata-deploy"
version = "0.1.0"
description = "Tests for deploy/kata config scripts (apply-config.sh toolset lockdown and session-pruning assertions)."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
