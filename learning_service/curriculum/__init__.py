"""Curriculum package (spec §Curriculum): `course`, `concept`,
`concept_edge`, `item`, `topic`, `topic_concept` — the concept graph and its
learner-facing item payloads. Owns `GET /topics`, `POST /admin/topics`,
`GET /me/concept-graph`, and `/admin/{courses,concepts,edges,items}` CRUD.
"""
