"""Identity package (spec §Identity, roles, teams): `person`, `identity`,
`link_code`, and resolving a platform identity to a person. Owns nothing
else — roster's manager tree and curriculum land in their own packages.
"""
