"""Team analytics (spec §Team analytics): subtree aggregates, adherence,
velocity, at-risk, recommendations, digests — plus the per-person learning
summary (retention bands, bypass rate, calibration) `/me/progress` and
`/team/people/{id}` both read, and `focus`/audit-log persistence for the
`/team/*` surface.
"""

from __future__ import annotations
